# Server
PORT=5000

# MySQL connection (matches schema.sql / phpMyAdmin database)
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=daniel12
DB_NAME=campusgo

# Admin login — pick your own strong password.
# POST /api/admin/login with { "password": "..." } to get an admin token,
# then pass it as `Authorization: Bearer <token>` on POST/PUT/DELETE routes.
ADMIN_PASSWORD=daniel12
 GOOGLE_CLIENT_ID=568817275949-uvdtdeeqfegmu6bjsafei1odlinlpohf.apps.googleusercontent.com